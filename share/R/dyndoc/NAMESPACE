exportPattern("^capture\\.")
export(
   
  try.cqls,
  init.filter.capture,
  filter.capture.output
)

#S3methods are not viewable inside the R system. If you want this functionality available put it in the previous export


